# Validar correcoes 085-090 - Editor, Pastas, Kanban, Hoje e Topbar

## Metadata

- Status: done
- Mode: research
- Complexity: medium
- Created: 2026-06-02
- Updated: 2026-06-02

## Objective

Validar (sem alterar codigo do app) se as correcoes enviadas ao Codex para os IDs
085, 086, 087, 088, 089 e 090 foram aplicadas corretamente em desktop e mobile.
Gerar prints de evidencia nomeados por ID/tela e registrar status por item.

## Context

- Correcoes implementadas pela spec `specs/2026-06-02-corrigir-085-090-ui-pastas-editor.md` (Status: done).
- ID 085 vive em `apps/web/src/app/(app)/notas/[id]/page.tsx` (container do editor: `max-w-[980px]`/`xl:max-w-[1040px]` normal e `max-w-[1180px]` no foco).
- ID 086 vive em `apps/web/src/components/folders/folder-options.tsx` (`flattenFolderOptions` com `localeCompare('pt-BR')`) e nos seletores de `item-detail.tsx`/`quick-capture.tsx` (busca fixa, lista rolavel `overflow-y-auto overscroll-contain`, botao `Cancelar selecao de pasta`).
- ID 087 e ID 089 vivem em `apps/web/src/app/(app)/notas/page.tsx` (overlay `Kanban em foco` + filtro `isHideCompleted` derivado da pasta aberta, linhas 802/823).
- ID 088 vive em `apps/web/src/app/(app)/today/today.css` (linhas 1041-1057: `box-shadow: none` nas linhas prio e cor de prioridade movida para a borda do `.task-check`).
- ID 090 vive em `apps/web/src/components/layout/topbar.tsx` (header `lg:h-auto`, `lg:py-0`, nav em pill separada) e `app-chrome.tsx`.
- Validacao independente via Playwright local, seed por API com usuario de teste.

## Scope

- [x] Validar ID 085 - largura util do editor normal e modo foco (desktop e mobile).
- [x] Validar ID 086 - seletor de pasta: scroll estavel, busca, ordem alfabetica, cancelar.
- [x] Validar ID 087 - modo foco do Kanban em tela cheia + sair do foco.
- [x] Validar ID 088 - alinhamento do marcador de prioridade na pagina Hoje.
- [x] Validar ID 089 - Kanban respeita `hideCompleted` da pasta aberta vs subpasta.
- [x] Validar ID 090 - topbar desktop com menos altura vertical.

## Out of scope

- Qualquer alteracao em codigo de app.
- Reabrir IDs ja validados (060-084).

## Grill Gate

Decision: not_needed

Reason:
Tarefa puramente de validacao, criterios objetivos definidos no pedido.

## Acceptance criteria (espelham o pedido)

- [x] ID 085: editor normal usa melhor a largura; modo foco amplia e remove trilhos; mobile nao quebra; sem largura exagerada em telas grandes.
- [x] ID 086: lista rola sem fechar, tem busca, ordem alfabetica hierarquica, permite cancelar sem alterar selecao.
- [x] ID 087: acao clara de foco, Kanban em tela cheia, saida clara do foco, colunas/cards/acoes preservados, modo lista intacto.
- [x] ID 088: risco vertical removido; prioridade clara pela borda do checkbox; checkboxes funcionam; desktop e mobile consistentes.
- [x] ID 089: pasta principal `ocultar concluidos` esconde concluidos das colunas mesmo de subpasta `mostrar concluidos`; entrar direto na subpasta respeita a regra propria; concluidos nao apagados.
- [x] ID 090: topbar desktop ocupa menos altura mantendo botoes confortaveis e alinhamento.

## Resultado (14/14 checks PASS)

| ID | Status | Observacao |
|---|---|---|
| 085 | OK | Editor normal wrapper 902px / prose 854px (viewport 1440); foco 1180px sem trilhos laterais; mobile 360px sem scroll horizontal. |
| 086 | OK | Seletor com busca, botao cancelar e lista rolavel (`overflow-y:auto`); ordem Alpha < Beta < Gamma < Zeta; busca filtra; cancelar fecha sem selecionar. |
| 087 | OK | Botao "Abrir Kanban em modo foco" abre overlay 1440x900 "KANBAN EM FOCO" com colunas/cards/+Adicionar; "Sair" retorna ao Kanban normal. |
| 088 | OK | Nenhuma linha com box-shadow (risco vertical removido); prioridade pela borda colorida do `.task-check` (P1 vermelho, P2 ambar); checkbox preservado desktop e mobile. |
| 089 | OK | Pasta principal Alpha (hideCompleted=true) oculta "Concluido beta" e mostra "Aberto beta"; ao entrar direto em Beta (hideCompleted=false) os concluidos aparecem. |
| 090 | OK | Topbar desktop com nav pill de 42px de altura, bottom 62px; botoes confortaveis e alinhados. |

## Progress

- 2026-06-02 - Lidas spec da correcao, AGENTS.md, e os arquivos-fonte dos IDs 085-090.
- 2026-06-02 - Confirmacao por leitura de codigo de que as 6 correcoes existem (max-width, sort alfabetico, overlay de foco, filtro hideCompleted da pasta aberta, box-shadow none + borda do checkbox, topbar compacta).
- 2026-06-02 - Servidor dev temporario em 127.0.0.1:3300; script de validacao independente `specs/validate-qa-085-090.mjs` criado e executado.
- 2026-06-02 - Inspecao visual dos prints principais confirmou os resultados programaticos.

## Validation

Commands run:

- [x] `pnpm --filter @doit/web exec next dev -p 3300 -H 127.0.0.1` (servidor temporario; encerrado via taskkill; porta 3300 livre)
- [x] `BASE_URL=http://127.0.0.1:3300 node specs/validate-qa-085-090.mjs` -> 14/14 PASS

Frontend evidence (`specs/artifacts/2026-06-02-validar-085-090/`):

- ID085-editor-largura-desktop.png, ID085-editor-foco-desktop.png, ID085-editor-mobile.png
- ID086-seletor-pasta-scroll-desktop.png, ID086-seletor-pasta-busca-desktop.png
- ID087-kanban-normal-desktop.png, ID087-kanban-foco-desktop.png
- ID088-hoje-alinhamento-desktop.png, ID088-hoje-alinhamento-mobile.png
- ID089-kanban-concluidos-pasta-desktop.png, ID089-subpasta-concluidos-desktop.png
- ID090-menu-superior-desktop.png
- resultados.json

## Risks

- Risk: prints podem expor dados reais da base local.
  Mitigation: usado usuario de teste com dados de seed.

## Next step

Validacao concluida; nenhum problema encontrado. Nenhuma alteracao de codigo do app foi feita.
Pronto para review/PR `dev` -> `main` da spec de correcao 085-090.
