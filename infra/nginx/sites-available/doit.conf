# Public production host for Doit.
# Replace DOIT_PUBLIC_DOMAIN before enabling this file.

server {
    listen 80;
    listen [::]:80;
    server_name DOIT_PUBLIC_DOMAIN;

    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name DOIT_PUBLIC_DOMAIN;

    ssl_certificate /etc/letsencrypt/live/DOIT_PUBLIC_DOMAIN/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/DOIT_PUBLIC_DOMAIN/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    client_max_body_size 10m;

    location /_next/static/ {
        proxy_pass http://127.0.0.1:8110;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    location = /sw.js {
        proxy_pass http://127.0.0.1:8110;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    location / {
        proxy_pass http://127.0.0.1:8110;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 60s;
    }
}
